#' # See \code{?example_data} to learn more about the
#' # \code{svm_res_} and \code{spline_res_} objects exported
#' # with the package. Long story short, they're example
#' # \code{tune_results} objects outputted from [tune::tune_grid()]!
#' @md